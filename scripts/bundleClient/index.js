// Generated by CoffeeScript 1.11.1
(function() {
  var PATHS, filePath, fs, i, len, path, paths, runScriptSync;

  PATHS = require('../PATHS');

  fs = require('fs-extra');

  path = require('path');

  runScriptSync = require('../util/runScriptSync');

  paths = ['./index.html', './res/fonts', './res/img', './res/js', './res/css'];

  console.log('BUILDING .coffee SCRIPTS...');

  runScriptSync(path.resolve(__dirname, '../compileCoffee'));

  console.log('BUILDING .less STYLESHEETS...');

  runScriptSync(path.resolve(__dirname, '../compileLess'));

  for (i = 0, len = paths.length; i < len; i++) {
    filePath = paths[i];
    fs.copySync(path.resolve(PATHS.CLIENT, filePath), path.resolve(PATHS.CLIENT_COMPILE_OUT, filePath));
  }

  console.log("EXTRACTED REQUIRED CLIENT FILES TO `" + PATHS.CLIENT_COMPILE_OUT + "`");

}).call(this);

//# sourceMappingURL=index.js.map
